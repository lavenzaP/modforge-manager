"""GUI entry point placeholder."""

from __future__ import annotations


def main() -> int:
    """Run the GUI shell.

    The first scaffold keeps PySide6 optional, so this entry point explains the
    current state instead of importing GUI dependencies unconditionally.
    """

    print("ModForge Manager GUI shell is not implemented yet.")
    print("Use `modforge --help` for the current CLI workflow.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
